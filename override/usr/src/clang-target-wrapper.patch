--- wrappers/clang-target-wrapper.sh
+++ wrappers/clang-target-wrapper.sh
@@ -38,6 +38,7 @@ esac
 case $ARCH in
 i686)
     # Dwarf is the default for i686.
+    FLAGS="$FLAGS -mmmx -msse -msse2 -mtune=pentium4"
     ;;
 x86_64)
     # SEH is the default for x86_64.
